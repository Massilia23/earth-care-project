
                   
                    <div class="card-block px-2">
                      <h4 class="card-title mt-3"><%= @mission.title %></h4>
                      <p class="card-text"><%= @mission.description %></p>
                      <p class="card-text"><%= @mission.duration %></p>
                      <p><% if current_user == @mission.user  %>
                        <%= link_to "Delete", mission_path(@bunker), data: {turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger" %>
                        <%# <% if policy(@bunker).edit? %>
                        <%= link_to "Edit your offer", edit_bunker_path(@mission.id), class: "btn btn-primary" %>
                        <%# <% end %>
                        <%= link_to "Dashboard", profil_path, data: {turbo_method: :get}, class: "btn btn-secondary" %>
                        <%# <%= link_to custom_url(:bunker_id => @bunker.id), class: "course-btn" do %>
                      <% end %>
